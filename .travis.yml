language: ruby
cache: bundler
rvm:
  - 2.0.0
  - 2.2.4
  - 2.3.0
# workaround for https://github.com/travis-ci/travis-ci/issues/5239
before_install:
  - gem install bundler
script: 'bundle exec rake rspec'
